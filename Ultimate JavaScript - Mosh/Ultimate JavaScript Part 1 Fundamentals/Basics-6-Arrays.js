//array is a data structure that we use to represent a list of items
//an array is a list of  of objects

//note use of meaningful variable name
let selectedColors = ['red', 'blue'];
selectedColors[2] = 1;
console.log(selectedColors);
//note a full stop after array name will display the use of available array properties
console.log(selectedColors.length);