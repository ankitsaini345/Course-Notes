//primitive types: strings, numbers boolean, undefined , null

let firstName = 'Astra'; // string literal
let age = 30; // number literal 
let isApproved = true; //  used for logic


//variable declared, not identified: undefined
//variable can explicity set to undefined, we don't use this often
// use null in situations when we want to clear the value of the variable
//example, user has not selected color, read example again.

let selectedColor = null;
//color selected, 




//ES6 another primitive is symbol